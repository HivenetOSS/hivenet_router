apiVersion: 1

datasources:
  - name: Loki
    type: loki
    uid: loki
    # "loki" resolves to the Loki container via Docker's internal network.
    url: http://loki:3100
    isDefault: false
    editable: false
