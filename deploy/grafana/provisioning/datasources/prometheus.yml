apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    # "prometheus" resolves to the Prometheus container via Docker's internal network .
    url: http://prometheus:9090
    isDefault: true
    editable: false
